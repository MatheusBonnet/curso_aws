language: bash
services: docker
before_install:
  - cd RestWithSpringBootUdemy/
  - echo "We are in the Docker folder!"
script:
  - docker-compose build
before_deploy:
  - echo "${DOCKER_PASSWORD}" | docker login --username "${DOCKER_USERNAME}" --password-stdin
deploy:
  provider: script
  script:
    docker push matheusbonnet/mysql-rest-with-spring-boot-udemy ;
    docker push matheusbonnet/rest_with_spring_boot_udemy;
  on:
    branch: master
notifications:
  email: false
